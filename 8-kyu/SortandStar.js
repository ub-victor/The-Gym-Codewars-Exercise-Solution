function twoSort(s) {
   let result =  s.sort()
   let final = result[0].plit().join("***")
    return final ;
}

console.log(twoSort(["bitcoin", "take", "over", "the", "world", "maybe", "who", "knows", "perhaps"]));