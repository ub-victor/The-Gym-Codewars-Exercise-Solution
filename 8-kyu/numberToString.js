const numberToString = function(num) {
  return String(num);
  // return num.toString(); // the toString method can also be used to convert a number to a string
  // return '' + num;      // concatenating an empty string with the number also converts it to a string
};


console.log(numberToString(1234)) // "1234" 